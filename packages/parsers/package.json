{
  "name": "@verder/parsers",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "//exports": "The barrel reaches SheetJS and builds Buffers at module scope — fine on the server, fatal in a browser bundle. The vault preview and the queue card are client components that need only the spreadsheet mime vocabulary, so that module is published as its own subpath.",
  "exports": {
    ".": "./src/index.ts",
    "./sheet-mimes": "./src/sheet-mimes.ts"
  },
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit", "build": "tsc --noEmit" },
  "dependencies": {
    "fast-xml-parser": "^4.5.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": { "@types/node": "^22.10.2", "vitest": "^2.1.8", "typescript": "^5.7.2" }
}
