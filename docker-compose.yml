services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: verder
      POSTGRES_PASSWORD: verder
      POSTGRES_DB: verder
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
